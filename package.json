{
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"build": "lerna run build",
		"build:watch": "lerna exec --parallel 'tsc -w'",
		"build:docs": "lerna exec ../../scripts/build-docs.sh",
		"test": "npm run test:tslint && npm run test:lint && npm run test:unit",
		"test:lint": "lerna run test:lint",
		"test:tslint": "lerna run test:tslint --concurrency 1",
		"test:tslint:fix": "lerna run test:tslint",
		"test:unit": "lerna run test:unit",
		"test:watch": "lerna run --parallel test:watch"
	},
	"devDependencies": {
		"lerna": "^2.9.0",
		"typedoc": "^0.10.0"
	}
}
